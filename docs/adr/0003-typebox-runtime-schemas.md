# TypeBox Runtime Schemas

Rowan uses TypeBox 1.x and compiled validators for runtime data that crosses model, store, tool, and provider seams because TypeScript types alone do not protect persisted JSON or model-produced structures.
