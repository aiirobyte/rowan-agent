# OpenAI-Compatible First Adapter

Rowan ships an OpenAI-compatible Chat Completions adapter first because it is enough to exercise the Agent loop, JSON output contracts, retry behavior, and provider normalization before broader provider IR work.
