# ContextScope Not Parallel Visibility

Rowan uses `ContextScope` on messages to distinguish conversation, execution, and diagnostic visibility. The project should strengthen this field instead of adding a parallel visibility property.
