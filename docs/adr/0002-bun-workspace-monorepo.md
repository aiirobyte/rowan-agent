# Bun Workspace Monorepo

Rowan is organized as a Bun workspace monorepo under `packages/*` so package ownership and dependency direction can be tested explicitly while the public surface is still evolving.
