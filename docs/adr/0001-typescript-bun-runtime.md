# TypeScript And Bun Runtime

Rowan uses TypeScript and Bun as its runtime foundation because the project is a local Agent harness that benefits from a fast script runner, native workspace support, and direct TypeScript execution during early iteration.
