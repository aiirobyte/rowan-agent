# Hooks Before Policy Engine

Rowan currently uses before and after tool hooks as the minimal policy surface. A fuller PolicyEngine is deferred until the runtime ToolRunner path is deep enough to support local tools, MCP tools, approvals, dry runs, and retries through one seam.
